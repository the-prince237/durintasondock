import { IconType } from 'react-icons';

export type ChildrenProp = {
  children: React.ReactNode;
};

export type NavLinkProps = { href: string; label: string };

export type SocialIconProps = { href: string; Icon: IconType };
